name = "ai-directory-v5-worker"
main = "worker/worker.js"
compatibility_date = "2024-01-01"

# Same D1 database as the previous project — no data migration needed.
[[d1_databases]]
binding = "DB"
database_name = "ai-directory-db"
database_id = "ff26faf5-3c7c-445a-a249-6c96fedddfdc"

[vars]
ENVIRONMENT = "production"
SITE_URL = "https://ai-directory-v5-radwan648.pages.dev"

# Secrets are set with `wrangler secret put` and are never stored in this file:
#   INTERNAL_API_KEY, TELEGRAM_BOT_TOKEN, ADMIN_TELEGRAM_ID
